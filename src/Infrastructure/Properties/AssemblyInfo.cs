using System.Runtime.CompilerServices;

// The bank-sync adapters are internal to this assembly and tested directly — the same arrangement
// Domain and Application already have.
[assembly: InternalsVisibleTo("Tests")]
